# md-tools-list

Collection of standalone Molecular dynamics (MD) analysis tools (Java).

Each tool is independent and located in its own `app-*` module.

## Tools

- app-CubeESPStationaryFitPar - ESP fitting from cube files
- app-NeighborStats - neighbor statistics

## Build all tools

```bash
gradle build
