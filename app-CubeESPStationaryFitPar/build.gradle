---

## `app-XYZ/build.gradle` (minimalus)

```gradle
plugins {
    id 'java'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

jar {
    archiveBaseName = 'app-CubeESPStationaryFitPar'
    manifest {
        attributes 'Main-Class': 'CubeESPStationaryFit'
    }
}
